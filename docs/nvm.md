# Node.js 版本管理指南 (NVM)

## 问题描述

当我们在不同前端项目间使用 Git 切换时，经常会遇到这样的错误：
`This project requires Node.js version X but you are using version Y`

这是因为不同的项目依赖不同版本的 Node.js，手动管理多个版本十分麻烦。

## 完美解决方案：使用 NVM 管理 Node 版本

**NVM (Node Version Manager)** 是一个 Node.js 版本管理工具，允许你在同一台机器上安装和切换不同版本的 Node.js。

### 🛠️ 安装 NVM (Node Version Manager)

#### Windows 用户
推荐使用第三方维护的 `nvm-windows`：
1.  下载安装包：访问 [nvm-windows releases](https://github.com/coreybutler/nvm-windows/releases) 下载 `nvm-setup.exe`。
2.  运行安装程序并按照提示完成安装。

#### macOS/Linux 用户

使用官方安装脚本：

```bash
# 执行安装命令
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.7/install.sh | bash

# 重启终端或加载配置
source ~/.bashrc  # 或 ~/.zshrc, ~/.profile
```

### 📥 安装指定 Node 版本

推荐使用 **Node.js 20.19.1**，该版本兼容 Vite 7 且稳定可靠：

```bash
# 安装指定版本
nvm install 20.19.1

# 切换到该版本
nvm use 20.19.1

# （可选）设置为默认版本，以后打开终端默认使用此版本
nvm alias default 20.19.1
```

### 💡 常用命令速查

*   `nvm list`: 列出本地已安装的所有 Node 版本。
*   `nvm use <version>`: 切换到指定版本（仅当前终端有效）。
*   `nvm install <version>`: 安装指定版本。
*   `nvm uninstall <version>`: 卸载指定版本。
*   `nvm current`: 显示当前正在使用的版本。
